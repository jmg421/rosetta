[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rosetta"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "rpy2>=3.5",
    "pandas>=1.5",
    "numpy>=1.23",
]

[project.optional-dependencies]
dev = ["pytest>=7"]
